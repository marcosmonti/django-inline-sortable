from setuptools import setup


setup(
    name = 'django-inline-sortable',
    version = '0.0.1',
    packages = ['isortable', ],
    author = 'Marcos Monti',
    author_email = 'marcosmonti@gmail.com',
    url = 'http://github.com/marcosmonti/django-inline-sortable/',
    license = 'GPLv3',
    description = 'Django app used for ordering inline formset elements using drag&drop',
    long_description = 'Django app used for ordering inline formset elements using drag&drop.',
    keywords = 'admin inline sortable python',
    install_requires = [],
)
